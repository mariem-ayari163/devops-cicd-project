name: Runner Test

on:
  workflow_dispatch:

jobs:

  test-runner:
    runs-on: self-hosted

    steps:

      - name: Test Self Hosted Runner
        run: |
          echo "===== RUNNER FONCTIONNE ====="
          echo "Hostname:"
          hostname
          echo "User:"
          whoami
          echo "Docker:"
          docker --version
          echo "Docker Compose:"
          